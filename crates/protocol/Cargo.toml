[package]
name = "phonepad-protocol"
version = "0.11.1"
edition = "2021"
license = "MIT"

[lib]
name = "phonepad_protocol"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1"
