[package]
name = "phonepad-protocol"
version = "0.4.0"
edition = "2021"
license = "MIT"

[lib]
name = "phonepad_protocol"
path = "src/lib.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
